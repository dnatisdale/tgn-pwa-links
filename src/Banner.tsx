// src/Banner.tsx
// Neutralized: prevents any accidental duplicate page banner rendering.
// Keep this file so imports won't break, but it renders nothing.
export default function Banner() {
  return null;
}
